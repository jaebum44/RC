#170816

//디바이스 드라이버 관련 파편지식

1. 커널컴파일 관련

cd /lib/modules/4.9.33-v7+/ 폴더 상에서 sudo ln -s /usr/src/linux/ build // => 소프트웨어 링크(바로가기 링크)
								            // => 커널 소스 다운만 받고 컴파일 안 한 경우에, 컴파일한 경우에는 필요 x

/lib/modules : 커널본체 kernel7.img <-> module의 경우 /lib/modules에 별도로 보관됨 



2. 커널소스 분석환경(vimrc)

ctags -R /home/subin/Desktop/gpiomodule // tags파일생성

cd ~/

vim .vimrc 

set tags+=/home/subin/Desktop/gpiomodule/tags //기존 tags 있는 경우에는 +=
					      //printk의 경우 해당폴더 찾아 등록시 전부 등록가능 

